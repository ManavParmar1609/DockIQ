---
version: 1
slug: "frontend-src-styles-app-css"
primary_target: "frontend/src/styles/app.css"
related_targets: ["frontend/src/pages/Landing.tsx"]
---

Scope: the whole DockIQ app shell and every screen (Operate), plus the landing page (Persuade). Visual world only; routes, flows, data and behaviour are unchanged. Light only.

Audience and job: dock workers on tablets (gloved, glare), supervisors triaging a zone, Quality holding product. Proof: the product's own screens with fictional, marked-simulated data.

## Direction contract

THESIS: Grafbase's engineering blueprint on cool marble, exactly — an API console that happens to run a dock. Refuses decoration: colour exists only in the announcement strip, in product content, and in severity.

OWN-WORLD: Drafting-gray #eaeaea canvas; white cards, 20px, 1px #e0e1e6 hairline, no shadow; graphite #1b1b1b text and the one primary action (6px, the single drop shadow, also on the preview panel); steel #60646c secondary; 12px panels and fields; 40px pills for tags and sign-in; the forest-to-deep-teal announcement strip; the cool triad (mint, sky, moss) inside diagrams, zones and charts only. Type: Playfair Display 400 display and section headings, tightened Inter for everything read, Hanken Grotesk captions, Geist Mono telemetry.

STORY: The worker sees what to do next and can close what they fixed; the supervisor sees what needs them in order; Quality sees what is held and why.

FIRST VIEWPORT: App — a white rail with the wordmark, the page title in the display serif, then the task's primary object before any counts. Landing — the strip, a white ruled nav with a sign-in pill and a graphite primary, a 50/50 hero: 90px headline, steel subtext, primary + ghost pair left; the product preview panel (sidebar + priority queue) right.

FORM: Owner-pinned Grafbase reference with the owner's type spec (no concept roll: the brief named the world). Seed key: owner-pinned.

FINISH: unreviewed and undocumented is unfinished; this build ends with the finish review, the verdict, DESIGN.md, and every shipping raster carrying its provenance
